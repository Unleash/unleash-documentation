{
  "name": "unleash-documentation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "fetch": "node scripts/fetch-openapi.mjs",
    "build:footer": "cd footer && npm install && npm run build",
    "dev": "npm run fetch && npm run build:footer && fern docs dev",
    "build": "npm run fetch && npm run build:footer && fern generate --docs"
  }
}
