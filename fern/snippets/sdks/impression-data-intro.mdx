When [impression data](/concepts/impression-data) is enabled for a flag, the SDK emits an `impression` event each time that flag is evaluated. Use impression events to track feature exposures in analytics tools.